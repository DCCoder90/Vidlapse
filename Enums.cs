using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace VidLapse {
    public enum CaptureMethod {
        App,
        Pixels,
        Tex
    }

    public enum SaveMethod {
        Memory,
        Disk
    }
}
